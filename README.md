# popvote

Zero-backend live audience polling (polls, word clouds, Q&A, ratings) that runs entirely in the presenter's browser tab. One HTML file. Audience joins via QR/link. No accounts, no server, no monthly meter.

Status: in development. See `RDD.md` for the spec and `HANDOFF.md` for current state.
